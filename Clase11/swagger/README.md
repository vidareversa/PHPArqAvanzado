# Swagger se instala por composer, quien genera nuestro openapi.json / openapi.yml

# Swagger UI se instala por composer también, quien es el encargado de mostrar nuestro json / yml de forma dinamica.